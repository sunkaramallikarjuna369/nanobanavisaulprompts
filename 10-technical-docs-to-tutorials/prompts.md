# Technical Docs to Tutorials Prompts

Transform technical documentation into visual learning guides.

---

## 91. API Quick-Start

```
Transform this API documentation into a visual quick-start guide with code snippets, expected responses, and common error solutions.
```

**Example:** Input: REST API docs → Output: 'GET /users' box with curl command, 200 response example, 404 error troubleshooting

---

## 92. Installation Wizard

```
Convert this installation guide into a visual step-by-step wizard with screenshots, command blocks, and success checkmarks.
```

**Example:** Input: Software install instructions → Output: Screen 1 with download button, Screen 2 with install wizard, checkmark at end

---

## 93. Annotated Config

```
Turn this configuration file into an annotated version with comment bubbles explaining each section and example values.
```

**Example:** Input: YAML config → Output: Config lines with arrow callouts: 'This sets port', 'Change this for prod', example values

---

## 94. Troubleshooting Tree

```
Create a troubleshooting decision tree from this support doc with questions, yes/no branches, and solution leaf nodes.
```

**Example:** Input: Error troubleshooting guide → Output: Diamond: 'Error code 500?' → Yes: 'Check logs' → No: 'Check network'

---

## 95. CLI Cheat Sheet

```
Transform this CLI reference into a cheat sheet with command syntax, option flags, and usage examples in organized boxes.
```

**Example:** Input: Git commands → Output: 'git commit -m' syntax, flag descriptions in table, example: git commit -m 'Fix bug'

---

## 96. ER Diagram

```
Convert this database schema into an ER diagram with tables, columns, primary keys, foreign keys, and relationships.
```

**Example:** Input: SQL CREATE statements → Output: Table boxes with PK underlined, FK with arrows to referenced tables

---

## 97. Security Checklist

```
Turn this security best practices doc into a checklist poster with icons, priority indicators, and quick action items.
```

**Example:** Input: Security guidelines → Output: Checkbox list with shield icons, High priority items first, action verbs

---

## 98. Deployment Pipeline

```
Create a deployment pipeline visualization from this DevOps doc with stages, tools used, and automated checks at each step.
```

**Example:** Input: CI/CD documentation → Output: Pipeline: Code → Build → Test → Deploy, tool icons at each stage, success/fail paths

---

## 99. Architecture Decision Matrix

```
Transform this architecture decision record into a comparison matrix with options evaluated against criteria with scores.
```

**Example:** Input: Tech stack decision doc → Output: Table: React vs Vue vs Angular rows, Performance/Community/Learning columns, scores

---

## 100. Onboarding Journey

```
Convert this onboarding document into a visual first-day journey map with timeline, tasks, people to meet, and resources.
```

**Example:** Input: New hire guide → Output: 9am: Setup laptop, 10am: Meet team (faces), 11am: First task, links to resources
